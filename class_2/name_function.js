function writeFullName(first, last) {
  document.write(`${first} ${last}`)
}

writeFullName("Ginnie", "Hench")
writeFullName("Hannah", "Hudson")
writeFullName("Jocelyn", "Triplett")
writeFullName("Eleven")


function writeFullName(first, last) {
  console.log("first is:" + first)
	console.log("last is:" + last)
	document.write(`${first} ${last}`)
}
